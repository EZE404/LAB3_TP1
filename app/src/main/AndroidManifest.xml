<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.albornoz.lab3_tp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LAB3_TP1">
        <activity
            android:name=".ui.registro.EditarActivity"
            android:exported="true"
            android:label="Editar Perfil"/>
        <activity
            android:name=".ui.registro.RegistroActivity"
            android:exported="true"
            android:label="Registrarse"/>
        <activity
            android:name=".ui.login.MainActivity"
            android:exported="true"
            android:label="Ingresar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>